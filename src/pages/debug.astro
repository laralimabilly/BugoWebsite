---
// Debug page to test if Astro is working
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bugo Debug</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            padding: 20px;
            background: #f5f5f5;
        }
        .debug-box {
            background: white;
            padding: 20px;
            margin: 20px 0;
            border-radius: 8px;
            border: 1px solid #ddd;
        }
        .success { border-left: 4px solid #68f70b; }
        .error { border-left: 4px solid #ff4444; }
        .warning { border-left: 4px solid #ffaa00; }
    </style>
</head>
<body>
    <h1>🔧 Bugo Debug Page</h1>
    
    <div class="debug-box success">
        <h3>✅ Astro is Working</h3>
        <p>This page loaded successfully, which means Astro is running correctly.</p>
    </div>
    
    <div class="debug-box">
        <h3>📁 File Check</h3>
        <p>Check if these files exist in your project:</p>
        <ul>
            <li><code>public/styles/base.css</code></li>
            <li><code>public/styles/components.css</code></li>
            <li><code>public/styles/sections.css</code></li>
            <li><code>public/styles/animations.css</code></li>
            <li><code>public/scripts/main.js</code></li>
            <li><code>public/scripts/animations.js</code></li>
        </ul>
    </div>
    
    <div class="debug-box">
        <h3>🧪 CSS Test</h3>
        <link rel="stylesheet" href="/styles/base.css">
        <div style="background: var(--green, red); color: white; padding: 10px; border-radius: 4px;">
            If this box is green, CSS is loading ✅
        </div>
    </div>
    
    <div class="debug-box">
        <h3>📜 JavaScript Test</h3>
        <div id="js-test" style="padding: 10px; background: #ffcccc; border-radius: 4px;">
            JavaScript not loaded ❌
        </div>
    </div>
    
    <div class="debug-box">
        <h3>🎬 GSAP Test</h3>
        <div id="gsap-test" style="padding: 10px; background: #ffcccc; border-radius: 4px;">
            GSAP not loaded ❌
        </div>
    </div>
    
    <script>
        // Test basic JavaScript
        document.getElementById('js-test').innerHTML = 'JavaScript is working ✅';
        document.getElementById('js-test').style.background = '#ccffcc';
        
        // Test GSAP loading
        function testGSAP() {
            if (typeof gsap !== 'undefined') {
                document.getElementById('gsap-test').innerHTML = 'GSAP is loaded ✅';
                document.getElementById('gsap-test').style.background = '#ccffcc';
            } else {
                setTimeout(testGSAP, 100);
            }
        }
        testGSAP();
        
        console.log('Debug page loaded');
        console.log('Document ready state:', document.readyState);
    </script>
    
    <!-- Load scripts to test -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="/scripts/animations.js" is:inline></script>
    <script src="/scripts/main.js" is:inline></script>
    
    <div class="debug-box warning">
        <h3>🚀 Next Steps</h3>
        <ol>
            <li>Make sure all files are in the correct locations</li>
            <li>Run <code>npm run dev</code></li>
            <li>Check browser console for errors (F12)</li>
            <li>Visit <code>/debug</code> to test components</li>
            <li>Visit <code>/</code> for the main site</li>
        </ol>
    </div>
    
    <p><a href="/">&larr; Back to Main Site</a></p>
</body>
</html>